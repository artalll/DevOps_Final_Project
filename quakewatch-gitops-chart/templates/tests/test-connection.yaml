apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quakewatch-gitops-chart.fullname" . }}-test-connection"
  labels:
    {{- include "quakewatch-gitops-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quakewatch-gitops-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
